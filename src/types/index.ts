export interface Customer {
  id: string; // Unique ID for each customer
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  state: string;
  isActive: boolean;
  details: string;
}
